import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
// 1. Tailwind utility layer + @theme tokens
import './index.css'
// 2. PMN master stylesheet (style.css) — diimport LANGSUNG di sini,
//    BUKAN di dalam index.css @layer base, agar tidak konflik dengan
//    Tailwind cascade. Vite akan bundle keduanya secara terpisah.
import '../style.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
